# Veb-server (FastAPI)
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12

# Baza
psycopg[binary]==3.2.3

# AI (vision va matn) qismi
requests==2.32.3
python-dotenv==1.0.1

# Skrinshot: CAPTURE_MODE="url" uchun (Playwright)
playwright==1.48.0

# Skrinshot: CAPTURE_MODE="window" yoki "screen" uchun
mss==9.0.2
pywin32==308
pillow==10.4.0

# Bashorat (predictor.py) — chiziqli regressiya va statistik hisoblar
numpy==2.1.3
